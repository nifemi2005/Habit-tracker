import { describe, test } from 'vitest'

describe('auth flow', () => {
  test.todo('submits the signup form and creates a session')
  test.todo('shows an error for duplicate signup email')
  test.todo('submits the login form and stores the active session')
  test.todo('shows an error for invalid login credentials')
})