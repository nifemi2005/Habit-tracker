import { describe, test } from 'vitest'

describe('habit form', () => {
  test.todo('shows a validation error when habit name is empty')
  test.todo('creates a new habit and renders it in the list')
  test.todo('edits an existing habit and preserves immutable fields')
  test.todo('deletes a habit only after explicit confirmation')
  test.todo('toggles completion and updates the streak display')
})